'use strict';

module.exports = function (_ref) {
  var user = _ref.user,
      pass = _ref.pass;
  return function (_ref2) {
    var options = _ref2.options,
        headers = _ref2.options.headers,
        body = _ref2.body;


    headers.Authorization = `Basic ${Buffer.from(`${user}:${pass || ''}`, 'utf8').toString('base64')}`;

    return { options, body };
  };
};