%h2 All Animals

%table#animals.table.table-striped.col-md-12 
  %thead
    %tr
      %th Tag
      %th Name
      %th Common Name
      %th Habitat #
      %th More Info
  %tbody
    - @animals.each do |animal|
      %tr
        %td= animal.tag
        %td= animal.name
        %td= animal.common_name
        %td= animal.habitat_num
        %td= link_to "More about #{animal.name}", animal_path(animal)


